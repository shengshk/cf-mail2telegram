cf-mail2telegram CF Worker notes
================================

Inbound mail → Gemini OTP (local regex fallback) → Telegram (Preview + Mailbox)


1) KV (required)
----------------

Bind a KV namespace to this Worker:

  Worker → Bindings → KV Namespace
  Variable name must be: DB


2) Variables
------------

Secrets (recommended):

  TELEGRAM_BOT       token,chat_id
  GEMINI_API_KEY     Google AI Studio key (AIza…)


Vars:

  UI_LANG            en (default) | zh | tw
                     Telegram labels, preview chrome, mini app, bot commands

  FORWARD_MAILS      Single backup only:
                     email[,GmailFolder][,noforwarded|forwarded]
                     e.g. you+bak@gmail.com,Backup,noforwarded
                     Default policy: noforwarded
                       — backup only mail addressed to the CF domain inbox
                       — skip mail auto-forwarded in from another mailbox
                     forwarded = also backup those auto-forwards
                     Hard rule (always): never backup when From / To-related
                     headers match the backup address (Gmail +tag normalized)

  DEBUG              true → show Gemini error on local OTP fallback


No DOMAIN variable.
Public host is saved when you open /init (KV key PUBLIC_HOST).


Mailbox button uses FORWARD_MAILS (and its folder if Gmail).


Defaults (usually leave alone):

  GEMINI_MODEL       gemini-2.5-flash-lite
  TIMEZONE           Asia/Shanghai
  GMAIL_U            0


Legacy (still read if FORWARD_MAILS unset):

  FORWARD_MAIL       same parse as FORWARD_MAILS (single address)
  FORWARD_LIST + FORWARD_DIR   first list address only

Ignored: FORWARD_MAIL0 / FORWARD_MAIL1 / …


3) Deploy
---------

keep_vars=true — dashboard plain Vars survive deploy.

After deploy open the Worker public URL (status page), then click the center status text to run /init
(orb → GitHub; blank area does nothing; 1.5s debounce). Or open .../init directly.

  Mode A (workers.dev — two levels before workers.dev):
    https://mail2telegram.your-subdomain.workers.dev

  Mode B (your own domain routed to this Worker):
    https://mail.example.com

JSON/toast should show host. Re-run /init after changing UI_LANG or switching primary host.

A and B can both exist on Cloudflare, but only the last successful /init host is used for Telegram/Preview/Mini App. Pick one primary; do not casually /init on the other.


4) Checklist
------------

  [ ] KV binding name DB
  [ ] TELEGRAM_BOT
  [ ] UI_LANG if you want zh or tw
  [ ] GEMINI_API_KEY for AI OTP
  [ ] FORWARD_MAILS verified in Email Routing destinations
  [ ] Email Routing → this Worker
  [ ] Visited /init (and removed any old DOMAIN var if present)
