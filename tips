mail2telegram CF Worker 配置备忘（本 fork）
==========================================

收信 → Gemini 抽验证码（失败才本地正则）→ Telegram（预览 + 邮箱）


一、KV（必须）
-------------

Bindings → KV Namespace，Variable name 必须是：DB


二、变量（精简）
---------------

Secrets（推荐）：

  TELEGRAM_BOT       token,chat_id
                     例：8557…:AAF-…,1639064318
                     （对齐 Docker；也兼容旧 TELEGRAM_TOKEN + TELEGRAM_ID）

  GEMINI_API_KEY     Google AIza… Key（要 AI 抽码时）


Vars：

  DOMAIN             例：mail2telegram.shengshk.workers.dev
                     （不要 https://）

  FORWARD_LIST       可选。备份邮箱，多个逗号分隔
                     例：shengshk+bak@gmail.com
                     须在 Email Routing「目标地址」里已验证


下面都有默认值，一般不用配：

  GEMINI_MODEL       gemini-2.5-flash-lite
  TIMEZONE           Asia/Shanghai
  GMAIL_U            0
  GMAIL_LABEL        INBOX
  MAIL_TTL / GUARDIAN_MODE / BLOCK_* / DEBUG / …


三、部署注意
-----------

wrangler 已 keep_vars=true：不会清空 Dashboard 明文变量。
密钥用 Secret 更稳。

部署后打开一次：https://你的DOMAIN/init


四、核对
-------

  [ ] KV 绑定名 DB
  [ ] TELEGRAM_BOT + DOMAIN
  [ ] GEMINI_API_KEY（要用 AI 抽码）
  [ ] FORWARD_LIST 若要用：目标地址已验证
  [ ] Email Routing → 该 Worker
  [ ] 访问过 /init
