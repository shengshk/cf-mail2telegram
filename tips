mail2telegram CF Worker 配置备忘
================================

收信 → Gemini 抽验证码（失败才本地正则）→ Telegram（预览 + 邮箱）


一、KV（必须）
-------------

光有 Workers KV 命名空间不够，还要绑到这个 Worker：

  Worker → 绑定 → KV Namespace
  变量名必须是：DB
  （本仓库 wrangler 已写 mail2telegram-db 的 id，部署后应自动带上）


二、变量（精简）
---------------

Secrets（推荐）：

  TELEGRAM_BOT       token,chat_id
                     格式同 Docker；也兼容旧 TELEGRAM_TOKEN + TELEGRAM_ID

  GEMINI_API_KEY     Google AI Studio Key（要 AI 抽码时）


Vars：

  DOMAIN             Worker 域名，不要带 https://
                     例：mail2telegram.<name>.workers.dev

  FORWARD_LIST       可选。备份邮箱，多个逗号分隔（取第一个决定「邮箱」按钮）
                     须在 Email Routing「目标地址」里已验证

  FORWARD_DIR        可选。仅 Gmail 有效：跳到该标签/文件夹（如 Backup）
                     非 Gmail 忽略；不配则 Gmail 跳首页

  DEBUG              可选。默认 false / 不配
                     true：TG 显示本地兜底时的 Gemini 错误原因；webhook 详日志
                     false：TG 不贴错误；无效 webhook 不刷 warn


「邮箱」按钮：有 thrid 优先精准；否则 Gmail+FORWARD_DIR→文件夹，
Gmail 无 DIR→首页；Outlook→outlook 首页；其它→https://mail.<域名>；
无 FORWARD_LIST 且无 FORWARD_DIR→只显示「预览」

TG 验证码样式：AI 加粗，本地斜体（无「· 本地」字样）


有默认、一般不用配：

  GEMINI_MODEL       gemini-2.5-flash-lite
  TIMEZONE           Asia/Shanghai
  GMAIL_U            0（Gmail 多账号时再改）
  MAIL_TTL / GUARDIAN_MODE / BLOCK_* / …


三、部署注意
-----------

wrangler 已 keep_vars=true：不会清空 Dashboard 明文变量。
密钥用 Secret 更稳。

部署后打开一次：https://<DOMAIN>/init


四、核对
-------

  [ ] KV 绑定名 DB
  [ ] TELEGRAM_BOT + DOMAIN
  [ ] GEMINI_API_KEY（要用 AI 抽码）
  [ ] FORWARD_LIST 若要用：目标地址已验证
  [ ] Email Routing → 该 Worker
  [ ] 访问过 /init
