{
	"name": "mail2telegram",
	"main": "src/index.ts",
	"compatibility_date": "2025-09-15",
	"compatibility_flags": [
		"nodejs_compat_v2"
	],
	"keep_vars": true,
	"kv_namespaces": [
		{
			"binding": "DB",
			"id": "YOUR_KV_NAMESPACE_ID"
		}
	],
	"vars": {
		"UI_LANG": "en"
		// Secrets (dashboard / wrangler secret):
		//   TELEGRAM_BOT=token,chat_id
		//   GEMINI_API_KEY=AIza...
		// Optional (single backup):
		//   FORWARD_MAIL=you+bak@gmail.com,Backup,noforwarded
		//   GMAIL_U=0
		// Preview: hard max 100 bodies; web link token 1d. /previewmode switches Mini App ↔ Web.
		// Public host: open https://<worker-host>/init once (saved to KV). No DOMAIN var.
	}
}
